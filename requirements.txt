# USD Impact Score v2 direct dependency specification.
#
# Production and CI install requirements.lock, which preserves the exact
# environment from a verified green weekly release. Use this file only as the
# human-maintained compatibility range for deliberate dependency upgrades.
# Python 3.11 is the production runtime.

pandas>=2.0
numpy>=1.24
yfinance>=0.2.40
matplotlib>=3.7
pyarrow>=14.0
