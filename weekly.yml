# Compatibility pointer only — this file is not executed by GitHub Actions.
#
# The authoritative Weekly USD Impact Score workflow is:
#   .github/workflows/weekly.yml
#
# Workflow definitions must remain inside .github/workflows/. Keeping this
# pointer prevents the obsolete direct-push workflow formerly stored here from
# being copied or mistaken for the guarded production process.
